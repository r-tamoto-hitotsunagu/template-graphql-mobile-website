import React, { memo } from 'react';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import type { {{ pascalCase feature_name }}Stacks } from './types';

export const {{ pascalCase feature_name }}Navigator = memo(() => {
  return (
      // TODO: implement!
  );
});

export * from './types';
