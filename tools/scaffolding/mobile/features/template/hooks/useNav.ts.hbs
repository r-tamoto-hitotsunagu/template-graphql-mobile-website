import { useNavigation } from '@react-navigation/native';
import type { {{ pascalCase feature_name }}Stacks } from '../navigators';
import type { NativeStackNavigationProp } from '@react-navigation/native-stack';

export const useNav = () => {
  return useNavigation<NativeStackNavigationProp<{{ pascalCase feature_name }}Stacks>>();
};
